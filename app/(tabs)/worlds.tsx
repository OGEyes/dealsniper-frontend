import { ComingSoon } from '@/components/ComingSoon';

export default function WorldsScreen() {
  return <ComingSoon title="Worlds" subtitle="World picker + per-world hub lands in the next milestone." />;
}
